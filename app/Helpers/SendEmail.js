'use strict'

const Mail = use('Mail');
const fs = use('fs');
const Helpers = use('Helpers');
const Base64 = use('js-base64').Base64;
const Puppeteer = use('puppeteer');
const Env = use('Env');
const Util = use('App/Helpers/Util')
class SendEmail {
    
}

module.exports = SendEmail;